# Historical baseline waiver

`HISTORICAL_PROVENANCE = PARTIAL_UNRECOVERABLE` remains in force for the legacy 26-record gap.

The missing identities came from the previous audit truncating its historical evidence with `.slice(0, 50)`. They are not evidence that 26 current competitions disappeared during this deployment, and they were not reconstructed or guessed.

This post-deployment snapshot starts a new complete regression baseline. It stores all public competition IDs and all memo IDs without truncation.

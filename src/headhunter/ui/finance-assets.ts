export const FINANCE_CSS = `
:root { color-scheme: light; --ink:#172033; --muted:#667085; --soft:#98a2b3; --line:#e6eaf0; --accent:#3157d5; --accent-soft:#eef2ff; --surface:#fff; --bg:#f6f8fb; --nav:#111a2b; --success:#16845b; --warning:#b54708; }
* { box-sizing:border-box; } body { margin:0; font-family:-apple-system,BlinkMacSystemFont,"PingFang SC","Microsoft YaHei",Inter,sans-serif; color:var(--ink); background:var(--bg); font-size:14px; } button,input { font:inherit; }
.finance-shell { min-height:100vh; display:flex; } .finance-nav { width:218px; flex:0 0 218px; padding:24px 14px 18px; background:var(--nav); color:#fff; display:flex; flex-direction:column; } .finance-brand { display:flex; align-items:center; gap:10px; padding:0 10px 30px; } .finance-brand-mark { width:30px; height:30px; display:grid; place-items:center; background:#fff; color:var(--nav); border-radius:9px; font-weight:800; } .finance-brand strong { display:block; font-size:15px; letter-spacing:-.02em; } .finance-brand small { display:block; margin-top:3px; color:#aeb9cd; font-size:11px; } .finance-nav-section { flex:1; } .finance-nav-label,.finance-eyebrow { color:#98a6bf; letter-spacing:.11em; text-transform:uppercase; font-size:10px; font-weight:700; } .finance-nav-label { padding:0 12px 9px; display:block; } .finance-nav a { color:#bdc7d8; text-decoration:none; display:flex; align-items:center; gap:11px; padding:11px 12px; border-radius:9px; margin:3px 0; transition:background .15s,color .15s; } .finance-nav a:hover { background:#1a2740; color:#fff; } .finance-nav a.active { background:#26395d; color:#fff; box-shadow:inset 3px 0 var(--accent); } .finance-nav-icon { width:20px; text-align:center; color:#91a4c4; font-size:13px; } .finance-nav a.active .finance-nav-icon { color:#c9d5ff; } .finance-nav-footer { border-top:1px solid #263248; padding:14px 10px 0; display:flex; align-items:center; gap:7px; color:#98a6bf; font-size:11px; } .finance-nav-footer a { padding:0; margin-left:auto; font-size:11px; color:#98a6bf; }
.finance-main { min-width:0; flex:1; padding:0 32px 42px; max-width:1480px; } .finance-topbar { min-height:72px; display:flex; justify-content:space-between; align-items:center; border-bottom:1px solid var(--line); gap:18px; } .finance-breadcrumb { display:flex; gap:9px; align-items:center; color:var(--soft); font-size:12px; } .finance-breadcrumb strong { color:var(--ink); font-weight:600; } .finance-top-actions { display:flex; align-items:center; gap:10px; } .finance-live-pill { display:inline-flex; align-items:center; gap:6px; padding:6px 9px; border:1px solid #cfe9dd; background:#f2fbf6; color:var(--success); border-radius:999px; font-size:11px; } .finance-live-pill i,.finance-status-dot { display:inline-block; width:6px; height:6px; border-radius:50%; background:#22a06b; } .finance-icon-button { border:1px solid var(--line); background:#fff; width:32px; height:32px; border-radius:8px; color:var(--muted); cursor:pointer; } .finance-button { border:0; border-radius:8px; background:var(--accent); color:#fff; padding:10px 15px; cursor:pointer; font-weight:650; box-shadow:0 1px 2px #3157d533; } .finance-button:hover { background:#2749bb; } .finance-button-small { padding:8px 12px; font-size:12px; }
.finance-hero { display:flex; justify-content:space-between; align-items:flex-end; gap:20px; padding:34px 0 22px; } .finance-hero h1,.finance-page-heading h1 { margin:7px 0 8px; font-size:30px; letter-spacing:-.04em; line-height:1.15; } .finance-hero p,.finance-page-heading p { margin:0; color:var(--muted); line-height:1.6; } .finance-hero-week { text-align:right; } .finance-hero-week strong,.finance-hero-week span { display:block; } .finance-hero-week strong { font-size:18px; } .finance-hero-week span { color:var(--muted); font-size:12px; margin-top:5px; } .finance-page-heading { padding:34px 0 22px; }
.finance-stat-row { display:grid; grid-template-columns:repeat(4,minmax(0,1fr)); gap:12px; margin-bottom:22px; } .finance-stat-row article { background:#fff; border:1px solid var(--line); border-radius:11px; padding:16px 17px; min-height:92px; } .finance-stat-row span,.finance-stat-row small { display:block; color:var(--muted); } .finance-stat-row span { font-size:12px; } .finance-stat-row strong { display:block; font-size:25px; margin:9px 0 2px; letter-spacing:-.03em; } .finance-stat-row small { font-size:11px; color:var(--soft); }
.finance-workspace { display:grid; grid-template-columns:minmax(0,1fr) 285px; align-items:start; gap:20px; } .finance-content { min-width:0; } .finance-insight { background:#fff; border:1px solid var(--line); border-radius:12px; padding:18px; position:sticky; top:18px; } .finance-insight-head { display:flex; justify-content:space-between; align-items:flex-start; margin-bottom:18px; } .finance-insight h3 { margin:6px 0 0; font-size:18px; } .finance-insight-badge { color:var(--accent); background:var(--accent-soft); border-radius:999px; font-size:10px; font-weight:700; padding:5px 7px; } .finance-insight-block { border-top:1px solid var(--line); padding:15px 0 2px; } .finance-insight-block h4 { margin:0 0 10px; font-size:12px; } .finance-insight-block p { font-size:12px; line-height:1.6; margin:0; } .finance-mini-list { margin:0; padding:0; list-style:none; } .finance-mini-list li { display:flex; justify-content:space-between; gap:9px; color:var(--muted); font-size:12px; padding:4px 0; } .finance-mini-list b { color:var(--ink); }
.finance-section-head { display:flex; justify-content:space-between; align-items:center; margin:0 0 10px; } .finance-section-head h3 { margin:0; font-size:17px; } .finance-section-head span { color:var(--muted); font-size:12px; } .finance-live-list { margin-top:4px; } .finance-lead-card { background:#fff; border:1px solid var(--line); border-radius:12px; padding:20px; margin:12px 0; } .finance-lead-card.is-a { border-color:#cbd5ff; box-shadow:0 5px 18px #1d39a30c; } .finance-lead-card.is-b { padding:15px 17px; } .finance-lead-header { display:flex; justify-content:space-between; gap:16px; align-items:flex-start; } .finance-lead-header h4 { margin:0; font-size:18px; letter-spacing:-.02em; } .finance-lead-card.is-b .finance-lead-header h4 { font-size:15px; } .finance-lead-header p { margin:6px 0 0; } .finance-lead-badge { display:inline-flex; align-items:center; gap:6px; white-space:nowrap; font-size:11px; font-weight:700; color:var(--accent); background:var(--accent-soft); border-radius:999px; padding:6px 9px; } .finance-lead-badge.b { background:#f2f4f7; color:#667085; } .finance-score { font-weight:700; color:var(--ink); } .finance-trigger { display:inline-flex; gap:6px; align-items:center; color:#7b61d1; font-size:11px; margin-top:11px; } .finance-trigger:before { content:""; width:6px; height:6px; border-radius:50%; background:#8f7be0; } .finance-lead-details { display:grid; grid-template-columns:108px minmax(0,1fr); gap:11px 16px; margin:19px 0 0; } .finance-lead-details dt { color:var(--muted); font-size:12px; font-weight:650; } .finance-lead-details dd { margin:0; line-height:1.6; } .finance-lead-card.is-b .finance-lead-details { grid-template-columns:72px minmax(0,1fr); margin-top:14px; gap:8px 12px; } .finance-lead-card.is-b .finance-lead-details dt:nth-of-type(n+3),.finance-lead-card.is-b .finance-lead-details dd:nth-of-type(n+3) { display:none; } .finance-chip-row { display:flex; flex-wrap:wrap; gap:6px; } .finance-chip { display:inline-flex; padding:4px 7px; border-radius:5px; background:#f2f4f7; color:var(--muted); font-size:11px; } .finance-chip.warn { color:var(--warning); background:#fff6ed; } .finance-evidence { border-top:1px solid var(--line); padding-top:12px; margin-top:14px; } .finance-evidence summary { cursor:pointer; color:var(--accent); font-size:12px; font-weight:600; } .finance-evidence ul { margin:10px 0 0; padding-left:18px; } .finance-evidence li { margin:6px 0; line-height:1.5; } .finance-evidence a { color:var(--ink); } .finance-evidence .finance-muted { font-size:11px; } .finance-lead-card footer { border-top:1px solid var(--line); padding-top:12px; margin-top:14px; color:var(--muted); font-size:11px; } .finance-lead-card blockquote { margin:0 0 9px; padding:10px 12px; background:#f8faff; border-left:3px solid #9aaefb; white-space:pre-wrap; } .finance-link { display:inline-flex; text-decoration:none; font-size:12px; padding:7px 9px; border:1px solid var(--line); color:var(--accent); background:#fff; border-radius:7px; } .finance-link.finance-button { border:0; color:#fff; } .finance-blockers { color:var(--warning); } .finance-empty { padding:32px 20px; border:1px dashed #cfd6e2; background:#fff; border-radius:11px; color:var(--muted); text-align:center; } .finance-loading { display:flex; align-items:center; justify-content:center; gap:9px; padding:48px; color:var(--muted); } .finance-spinner { width:15px; height:15px; border:2px solid #d9e0ee; border-top-color:var(--accent); border-radius:50%; animation:finance-spin .8s linear infinite; } @keyframes finance-spin { to { transform:rotate(360deg); } }
.finance-table { width:100%; border-collapse:collapse; background:#fff; border:1px solid var(--line); border-radius:11px; overflow:hidden; } .finance-table th,.finance-table td { text-align:left; padding:13px 15px; border-bottom:1px solid var(--line); font-size:12px; } .finance-table th { color:var(--muted); font-weight:600; background:#fbfcfe; } .finance-table tr:last-child td { border-bottom:0; } .finance-login { max-width:420px; margin:12vh auto; } .finance-login h2 { margin:8px 0; } .finance-login form { margin-top:26px; } .finance-login label { display:block; color:var(--muted); font-size:12px; margin:15px 0; } .finance-login input { display:block; width:100%; padding:11px; border:1px solid var(--line); border-radius:8px; margin-top:7px; background:#fff; } .finance-login .finance-button { width:100%; margin-top:8px; }
.finance-evidence-layers { display:grid; grid-template-columns:repeat(4,minmax(0,1fr)); gap:9px; border-top:1px solid var(--line); margin-top:15px; padding-top:13px; } .finance-evidence-layer { background:#f8faff; border-radius:8px; padding:10px; min-width:0; } .finance-evidence-layer strong { display:block; color:var(--muted); font-size:11px; margin-bottom:5px; } .finance-evidence-layer span { display:block; font-size:12px; line-height:1.5; }
.finance-action-center { margin:0 0 20px; padding:20px 22px; background:#fffdf7; border:1px solid #d8c9a9; border-radius:12px; } .finance-action-grid { display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:12px; } .finance-action-card { border:1px solid #e2d8c4; border-radius:10px; padding:15px; background:#fff; min-height:154px; display:flex; flex-direction:column; gap:8px; } .finance-action-card h4 { margin:0; font:600 16px/1.35 Georgia,serif; } .finance-action-card p { margin:0; color:#566258; font-size:12px; line-height:1.55; } .finance-action-card .finance-action-meta { color:#728071; font-size:11px; } .finance-action-card .finance-link { margin-top:auto; align-self:flex-start; } .finance-detail-heading { padding-bottom:12px; } .finance-workspace-single { display:block; } .finance-detail-page { display:grid; gap:16px; } .finance-detail-section { background:#fff; border:1px solid var(--line); border-radius:12px; padding:20px; } .finance-detail-section h3 { margin:0 0 12px; font-size:16px; } .finance-detail-section p { margin:0; color:#475467; line-height:1.7; } .finance-detail-grid { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:14px; } .finance-detail-actions { display:flex; flex-wrap:wrap; gap:10px; align-items:center; } .finance-detail-actions select,.finance-detail-actions textarea { border:1px solid var(--line); border-radius:7px; padding:9px; background:#fff; } .finance-detail-actions textarea { min-width:260px; min-height:70px; } .finance-detail-note { color:var(--success); font-size:12px; } .finance-detail-accordion { margin-top:13px; border-top:1px solid #ddd6c7; padding-top:11px; } .finance-detail-accordion summary { cursor:pointer; color:#6b7e67; font-size:12px; font-weight:650; } .finance-detail-accordion .finance-evidence-layers { border-top:0; margin-top:9px; padding-top:0; } .finance-detail-contact-list,.finance-detail-evidence-list { margin:0; padding-left:18px; color:#475467; line-height:1.8; } .finance-admin-panel { margin-top:0; } .finance-about-card { display:grid; grid-template-columns:230px minmax(0,1fr); gap:24px; margin:0 0 20px; padding:22px 24px; background:#f5f0e5; border:1px solid #d8cdb8; } .finance-about-card h2 { margin:7px 0 0; font:500 22px Georgia,serif; color:#2b302d; } .finance-about-copy p { margin:0; color:#4f5a52; line-height:1.75; } .finance-about-points { display:grid; gap:0; margin-top:14px; border-top:1px solid #d8cdb8; } .finance-about-points div { display:flex; gap:14px; padding:10px 0; border-bottom:1px solid #d8cdb8; color:#3f4b43; font-size:12px; } .finance-about-points b { color:#b87535; font:500 16px Georgia,serif; } .finance-pagination { display:flex; align-items:center; justify-content:flex-end; gap:10px; margin:16px 0 0; color:#687568; font-size:12px; } .finance-pagination select { border:1px solid #d5dcca; border-radius:6px; padding:6px 8px; background:#fff; color:#465449; } .finance-pagination button { border:1px solid #d5dcca; border-radius:6px; padding:6px 10px; background:#fff; color:#465449; cursor:pointer; } .finance-pagination button:disabled { opacity:.45; cursor:default; }
@media (max-width:980px) { .finance-main { padding:0 20px 32px; } .finance-workspace { grid-template-columns:minmax(0,1fr); } .finance-insight { position:static; order:-1; } .finance-insight-block { display:inline-block; width:31%; vertical-align:top; border-top:0; padding:0 10px 0 0; } }
@media (max-width:720px) { .finance-shell { display:block; } .finance-nav { width:auto; padding:12px 12px 10px; position:sticky; top:0; z-index:4; } .finance-brand { padding:0 6px 11px; } .finance-nav-section { display:flex; overflow:auto; gap:2px; } .finance-nav-label,.finance-nav-footer { display:none; } .finance-nav a { flex:0 0 auto; padding:8px 9px; margin:0; font-size:12px; } .finance-nav-icon { display:none; } .finance-main { padding:0 14px 28px; } .finance-topbar { min-height:58px; } .finance-breadcrumb span:first-child,.finance-live-pill { display:none; } .finance-top-actions { margin-left:auto; } .finance-hero,.finance-page-heading { padding:24px 0 16px; } .finance-hero h1,.finance-page-heading h1 { font-size:25px; } .finance-hero-week { display:none; } .finance-stat-row { grid-template-columns:repeat(2,minmax(0,1fr)); gap:8px; margin-bottom:16px; } .finance-stat-row article { min-height:83px; padding:12px; } .finance-stat-row strong { font-size:22px; margin-top:8px; } .finance-insight { padding:14px; } .finance-insight-block { display:block; width:auto; padding:11px 0; border-top:1px solid var(--line); } .finance-lead-card { padding:15px; } .finance-lead-header { gap:8px; } .finance-lead-header h4 { font-size:16px; } .finance-lead-badge { padding:5px 7px; font-size:10px; } .finance-lead-details,.finance-lead-card.is-b .finance-lead-details { grid-template-columns:1fr; gap:5px; margin-top:15px; } .finance-lead-details dt { margin-top:7px; } .finance-lead-details dd { font-size:13px; } .finance-table { display:block; overflow-x:auto; white-space:nowrap; } }
.finance-executive-shell { display:block; background:#f7f5ef; color:#1c201f; }
.finance-executive-shell .finance-nav { display:none; }
.finance-executive-shell .finance-main { max-width:none; padding:0 32px 48px; }
.finance-executive-shell .finance-topbar { min-height:88px; border-bottom:1px solid #28302e; background:#101615; color:#f5f0e4; padding:0 24px; }
.finance-executive-shell .finance-brand-inline { display:flex; flex-direction:column; gap:5px; margin-right:35px; white-space:nowrap; }
.finance-executive-shell .finance-brand-inline strong { font:500 19px Georgia,serif; letter-spacing:.01em; }
.finance-executive-shell .finance-brand-inline small { color:#d3c8b3; font-size:11px; }
.finance-executive-shell .finance-executive-nav { display:flex; gap:28px; flex:1; }
.finance-executive-shell .finance-executive-nav a { color:#d9d2c5; text-decoration:none; font:500 15px Georgia,serif; padding:32px 0 25px; border-bottom:3px solid transparent; }
.finance-executive-shell .finance-executive-nav a.active { color:#e6c778; border-color:#e6c778; }
.finance-executive-shell .finance-breadcrumb { display:none; }
.finance-executive-shell .finance-top-actions .finance-button-small { display:none; }
.finance-executive-shell .finance-icon-button { background:transparent; border-color:#62645d; color:#f2eadb; }
.finance-executive-shell .finance-live-pill { background:transparent; border-color:#526454; color:#d4e1cf; }
.finance-executive-shell .finance-hero { margin:0 -32px; padding:42px 64px 39px; background:linear-gradient(110deg,#101615 0%,#121b1a 54%,#182221 100%); color:#f7f0e2; align-items:center; }
.finance-executive-shell .finance-hero-copy { flex:1; }
.finance-executive-shell .finance-eyebrow { color:#d6b866; letter-spacing:.12em; }
.finance-executive-shell .finance-hero h1 { font:500 clamp(42px,5vw,68px)/1.05 Georgia,serif; letter-spacing:-.035em; margin:18px 0 17px; }
.finance-executive-shell .finance-hero h1 em { color:#e2bd67; font-style:normal; }
.finance-executive-shell .finance-hero p { color:#d8d3c6; font-size:14px; line-height:1.8; }
.finance-hero-callout { width:370px; border:1px solid #756741; padding:22px 24px 18px; display:grid; grid-template-columns:50px 1fr; gap:10px 14px; }
.finance-compass { width:42px; height:42px; border:2px solid #e0bb62; border-radius:50%; display:grid; place-items:center; color:#e0bb62; font-size:23px; }
.finance-hero-callout strong { font:500 18px Georgia,serif; color:#e6c778; }
.finance-hero-callout p { margin:9px 0 0; color:#d6d1c4; font-size:13px; line-height:1.7; }
.finance-hero-actions { grid-column:1 / -1; display:flex; gap:10px; margin-top:8px; }
.finance-hero-actions .finance-button { background:#e5c372; color:#1c201f; border-radius:5px; flex:1; font:600 15px Georgia,serif; }
.finance-hero-actions .finance-link { background:transparent; border-color:#aa8e4f; color:#f2db9b; padding:10px 14px; }
.finance-signal-ribbon { margin:20px 0 26px; border:1px solid #d4ccb9; background:#fbfaf6; padding:13px 20px; display:flex; align-items:center; gap:20px; color:#48564b; font-size:12px; }
.finance-signal-ribbon strong { font:500 16px Georgia,serif; color:#344b3a; white-space:nowrap; }
.finance-signal-ribbon span { background:#eef1e8; border-radius:999px; padding:6px 10px; white-space:nowrap; }
.finance-signal-ribbon b { margin-left:auto; color:#668067; white-space:nowrap; font-weight:500; }
.finance-executive-shell .finance-stat-row { display:none; }
.finance-executive-shell .finance-workspace { grid-template-columns:minmax(0,1fr) 300px; }
.finance-executive-shell .finance-section-head { border-bottom:1px solid #d5cdbb; padding:0 12px 13px; }
.finance-executive-shell .finance-section-head h3 { font:500 21px Georgia,serif; }
.finance-executive-shell .finance-section-head span { color:#7a806e; }
.finance-executive-shell .finance-lead-card { border:0; border-bottom:1px solid #ddd6c7; border-radius:0; background:transparent; margin:0; padding:22px 12px; display:grid; grid-template-columns:1.05fr 1.25fr 1.15fr 1fr; gap:24px; }
.finance-executive-shell .finance-lead-card.is-a { box-shadow:none; }
.finance-executive-shell .finance-lead-header { display:block; }
.finance-executive-shell .finance-lead-header h4 { font:600 17px/1.4 Georgia,serif; color:#202a25; }
.finance-executive-shell .finance-lead-header p { color:#758073; font-size:12px; }
.finance-executive-shell .finance-lead-badge { margin-top:11px; background:#e8f0e6; color:#54755a; border-radius:999px; }
.finance-executive-shell .finance-trigger { color:#708a69; margin-top:13px; }
.finance-executive-shell .finance-lead-details { display:grid; grid-column:2 / -1; grid-template-columns:1.08fr 1fr 1fr; gap:18px; margin:0; align-items:start; }
.finance-executive-shell .finance-lead-field { min-width:0; }
.finance-executive-shell .finance-lead-field dt { margin:0 0 8px; }
.finance-executive-shell .finance-lead-field dd { margin:0; }
.finance-executive-shell .finance-lead-field:nth-child(3),.finance-executive-shell .finance-lead-field:nth-child(4) { grid-column:3; }
.finance-executive-shell .finance-lead-field:nth-child(n+5) { display:none; }
.finance-executive-shell .finance-lead-details dt { color:#7b8278; font-size:11px; }
.finance-executive-shell .finance-lead-details dd { color:#303b34; font-size:13px; line-height:1.6; }
.finance-executive-shell .finance-lead-details dt:nth-of-type(1),.finance-executive-shell .finance-lead-details dd:nth-of-type(1) { grid-column:2; }
.finance-executive-shell .finance-lead-details dt:nth-of-type(2),.finance-executive-shell .finance-lead-details dd:nth-of-type(2) { grid-column:3; }
.finance-executive-shell .finance-lead-details dt:nth-of-type(3),.finance-executive-shell .finance-lead-details dd:nth-of-type(3) { grid-column:4; }
.finance-executive-shell .finance-lead-details dt:nth-of-type(4),.finance-executive-shell .finance-lead-details dd:nth-of-type(4) { grid-column:4; }
.finance-executive-shell .finance-lead-details dt:nth-of-type(n+5),.finance-executive-shell .finance-lead-details dd:nth-of-type(n+5),.finance-executive-shell .finance-evidence,.finance-executive-shell .finance-lead-card footer { display:none; }
.finance-executive-shell .finance-evidence-layers { grid-column:1 / -1; border-top:1px solid #ddd6c7; }
.finance-executive-shell .finance-insight { background:#f5f1e8; border:1px solid #d2c7ae; border-radius:0; }
.finance-executive-shell .finance-insight h3 { font:500 20px Georgia,serif; }
@media (max-width:900px) { .finance-executive-shell .finance-executive-nav { gap:12px; } .finance-executive-shell .finance-executive-nav a { font-size:12px; } .finance-hero-callout { width:330px; } .finance-signal-ribbon { overflow:auto; } .finance-executive-shell .finance-lead-card { grid-template-columns:1fr 1fr; } .finance-executive-shell .finance-lead-details { grid-column:1 / -1; grid-template-columns:1fr 1fr; } .finance-executive-shell .finance-lead-field:nth-child(3),.finance-executive-shell .finance-lead-field:nth-child(4) { grid-column:auto; } }
@media (max-width:650px) { .finance-executive-shell .finance-main { padding:0 14px 32px; } .finance-executive-shell .finance-topbar { margin:0 -14px; padding:0 14px; } .finance-executive-shell .finance-brand-inline { margin-right:12px; } .finance-executive-shell .finance-brand-inline strong { font-size:15px; } .finance-executive-shell .finance-executive-nav { display:none; } .finance-executive-shell .finance-hero { margin:0 -14px; padding:30px 24px; display:block; } .finance-hero-callout { width:auto; margin-top:25px; } .finance-signal-ribbon { margin:14px 0 18px; } .finance-signal-ribbon span { display:none; } .finance-executive-shell .finance-lead-card { display:block; padding:18px 5px; } .finance-executive-shell .finance-lead-details { display:grid; grid-template-columns:90px 1fr; margin-top:14px; } .finance-executive-shell .finance-lead-field:nth-child(n) { grid-column:auto; } .finance-executive-shell .finance-lead-details dt:nth-of-type(n+5),.finance-executive-shell .finance-lead-details dd:nth-of-type(n+5) { display:none; } }
@media (max-width:900px) { .finance-evidence-layers { grid-template-columns:repeat(2,minmax(0,1fr)); } .finance-action-grid,.finance-detail-grid { grid-template-columns:1fr; } }
@media (max-width:720px) { .finance-evidence-layers { grid-template-columns:1fr; } }
`;

/** Executive Brief interaction layer: action center, progressive disclosure and opportunity detail. */
export const FINANCE_ACCEPTANCE_JS = `
(function () {
  const esc = (value) => String(value ?? '').replace(/[&<>"']/g, (char) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
  const json = (path, options) => fetch(path, options).then((response) => { if (!response.ok) throw new Error(String(response.status)); return response.json(); });
  const enhance = () => document.querySelectorAll('.finance-lead-card').forEach((card) => {
    const layers = card.querySelector('.finance-evidence-layers');
    if (card.querySelector('.finance-detail-accordion')) return;
    const trigger = card.querySelector('.finance-trigger')?.textContent?.trim() || '近期公开信号';
    const values = [...card.querySelectorAll('.finance-lead-details dt')].reduce((map, dt) => { map[dt.textContent.trim()] = dt.nextElementSibling?.textContent?.trim() || ''; return map; }, {});
    const details = card.querySelector('.finance-lead-details');
    if (details && !details.querySelector('.finance-lead-field')) {
      const nodes = [...details.children];
      for (let index = 0; index < nodes.length; index += 2) {
        const field = document.createElement('div'); field.className = 'finance-lead-field';
        if (nodes[index]) field.appendChild(nodes[index]); if (nodes[index + 1]) field.appendChild(nodes[index + 1]); details.appendChild(field);
      }
    }
    const layerSection = layers || document.createElement('section'); layerSection.className = 'finance-evidence-layers'; layerSection.setAttribute('aria-label', '事实与判断分层');
    layerSection.innerHTML = '<div class="finance-evidence-layer"><strong>业务动因</strong><span>' + esc(trigger) + '</span></div><div class="finance-evidence-layer"><strong>事实依据</strong><span>' + esc(trigger) + '</span></div><div class="finance-evidence-layer"><strong>系统判断</strong><span>' + esc(values['为什么现在'] || '待人工复核') + '</span></div><div class="finance-evidence-layer"><strong>业务建议</strong><span>' + esc(values['本周行动'] || values['潜在需求'] || '待人工复核') + '</span></div>';
    const wrapper = document.createElement('details'); wrapper.className = 'finance-detail-accordion'; wrapper.innerHTML = '<summary>展开证据与判断</summary>';
    if (layers) layers.parentNode.insertBefore(wrapper, layers); else card.appendChild(wrapper); wrapper.appendChild(layerSection);
  });
  const renderActionCenter = async () => {
    const mount = document.querySelector('#finance-action-center .finance-action-grid'); if (!mount) return;
    try {
      const snapshot = await json('/api/finance/weekly/current'); const leads = (snapshot.leads || []).slice().sort((a, b) => (b.final_rank_score || 0) - (a.final_rank_score || 0)).slice(0, 3);
      let opportunities = []; try { opportunities = await json('/api/finance/opportunities'); } catch (_) {}
      const byCompany = new Map(opportunities.map((item) => [item['company_'+'id'], item]));
      mount.innerHTML = leads.length ? leads.map((lead) => { const opportunity = byCompany.get(lead['company_'+'id']); const detailId = opportunity?.opportunity_id || ('lead-' + lead.id); const contacts = (lead.contacts || []).filter((item) => item.url || item.email || item.phone); const person = contacts[0]; const action = lead.manual_action || lead.bd_action_zh || lead.generated_action || '安排一次 15 分钟确认沟通'; return '<article class="finance-action-card"><div class="finance-action-meta">' + esc(lead.lead_pool === 'A_ACTIONABLE' ? 'A级机会' : 'B级情报') + ' · ' + esc(lead.region || '区域待复核') + '</div><h4>' + esc(lead.company_name || '未命名公司') + '</h4><p><strong>联系人：</strong>' + esc(person?.name || '先确认决策链联系人') + '</p><p><strong>原因：</strong>' + esc(lead.why_now_zh || lead.opportunity_summary || '近期出现值得跟进的公开信号') + '</p><p><strong>下一步：</strong>' + esc(action) + '</p><a class="finance-link" href="/opportunities/' + encodeURIComponent(detailId) + '">查看并执行 →</a></article>'; }).join('') : '<div class="finance-empty">本周暂无可执行行动</div>';
    } catch (_) { mount.innerHTML = '<div class="finance-empty">今日行动暂不可用，请刷新重试</div>'; }
  };
  const setupPagination = () => {
    const live = document.querySelector('#finance-live-data'); if (!live || live.querySelector('.finance-pagination')) return;
    const cards = [...live.querySelectorAll('.finance-lead-card')]; if (!cards.length) return;
    const pagination = document.createElement('div'); pagination.className = 'finance-pagination'; pagination.innerHTML = '<span data-page-summary></span><label>每页 <select aria-label="每页显示条数"><option value="5">5</option><option value="10">10</option><option value="30">30</option></select></label><button type="button" data-page-prev>上一页</button><button type="button" data-page-next>下一页</button>';
    live.appendChild(pagination); const select = pagination.querySelector('select'); const summary = pagination.querySelector('[data-page-summary]'); let page = 1;
    const render = () => { const size = Number(select.value); const pages = Math.max(1, Math.ceil(cards.length / size)); page = Math.min(page, pages); cards.forEach((card, index) => { card.style.display = index >= (page - 1) * size && index < page * size ? '' : 'none'; }); summary.textContent = '第 ' + page + ' / ' + pages + ' 页 · 共 ' + cards.length + ' 条'; pagination.querySelector('[data-page-prev]').disabled = page <= 1; pagination.querySelector('[data-page-next]').disabled = page >= pages; };
    select.addEventListener('change', () => { page = 1; render(); }); pagination.querySelector('[data-page-prev]').addEventListener('click', () => { page -= 1; render(); }); pagination.querySelector('[data-page-next]').addEventListener('click', () => { page += 1; render(); }); render();
  };
  const renderDetail = async () => {
    const live = document.querySelector('#finance-live-data'); if (!live || !location.pathname.startsWith('/opportunities/')) return;
    const id = decodeURIComponent(location.pathname.slice('/opportunities/'.length));
    try {
      const item = await json('/api/finance/opportunities/' + encodeURIComponent(id));
      let lead = null; try { const snapshot = await json('/api/finance/weekly/current'); lead = (snapshot.leads || []).find((candidate) => candidate['company_'+'id'] === item['company_'+'id']); } catch (_) {}
      const contacts = (lead?.contacts || []).filter((contact) => contact.url || contact.email || contact.phone); const evidences = (lead?.evidences || []).filter((evidence) => evidence.source_url);
      const script = lead?.manual_outreach || lead?.first_touch_script_zh || lead?.generated_outreach || '暂无首触话术';
      live.innerHTML = '<div class="finance-detail-page"><section class="finance-detail-section"><div class="finance-eyebrow">' + esc(item.status || 'discovered') + ' · SCORE ' + esc(item.score) + '</div><h2>' + esc(lead?.company_name || item.title || '机会') + '</h2><p>' + esc(lead?.industry || '') + (lead?.region ? ' · ' + esc(lead.region) : '') + '</p></section><section class="finance-detail-section"><h3>为什么现在</h3><p>' + esc(item.why_now || lead?.why_now_zh || '待补充') + '</p><div class="finance-detail-grid"><div><h3>业务动因</h3><p>' + esc(item.business_driver || lead?.business_driver_zh || '待补充') + '</p></div><div><h3>人才需求</h3><p>' + esc(item.talent_need || lead?.talent_need_zh || '待补充') + '</p></div></div></section><section class="finance-detail-section"><h3>推荐联系人</h3>' + (contacts.length ? '<ul class="finance-detail-contact-list">' + contacts.map((contact) => '<li>' + esc(contact.name || contact.title || '联系人') + ' ' + (contact.url ? '<a class="finance-link" href="' + esc(contact.url) + '" target="_blank" rel="noreferrer">LinkedIn</a>' : '') + '</li>').join('') + '</ul>' : '<p>暂无可验证联系入口</p>') + '</section><section class="finance-detail-section"><h3>证据链</h3>' + (evidences.length ? '<ul class="finance-detail-evidence-list">' + evidences.map((evidence) => '<li><a href="' + esc(evidence.source_url) + '" target="_blank" rel="noreferrer">' + esc(evidence.title || '查看原文') + '</a></li>').join('') + '</ul>' : '<p>暂无可验证原文</p>') + '</section><section class="finance-detail-section"><h3>首触建议</h3><blockquote>' + esc(script) + '</blockquote><div class="finance-detail-actions"><button class="finance-button" data-copy="' + esc(script) + '">复制首触话术</button><span class="finance-detail-note" data-detail-note></span></div></section><section class="finance-detail-section"><h3>跟进记录</h3><form data-opportunity-form><div class="finance-detail-actions"><select name="status"><option value="discovered">待确认</option><option value="verified">已验证</option><option value="ready_to_contact">待联系</option><option value="contacted">已联系</option><option value="in_follow_up">跟进中</option><option value="completed">已完成</option><option value="archived">已归档</option></select><textarea name="note" placeholder="添加本次跟进备注"></textarea><button class="finance-button" type="submit">保存跟进</button></div><p class="finance-detail-note" data-form-message></p></form></section></div>';
      const form = live.querySelector('[data-opportunity-form]'); const select = form.querySelector('select'); select.value = item.status || 'discovered'; form.addEventListener('submit', async (event) => { event.preventDefault(); const message = form.querySelector('[data-form-message]'); try { const response = await fetch('/api/finance/opportunities/' + encodeURIComponent(id) + '/status', { method:'PATCH', headers:{'content-type':'application/json'}, body:JSON.stringify({ status: select.value, note: form.note.value }) }); if (response.status === 401) throw new Error('请先登录管理账号'); if (!response.ok) throw new Error('保存失败（' + response.status + '）'); message.textContent = '已保存跟进记录'; form.note.value = ''; } catch (error) { message.textContent = error.message || '保存失败'; } });
    } catch (error) { live.innerHTML = '<div class="finance-empty">机会详情加载失败：' + esc(error.message || '未知错误') + '</div>'; }
  };
  const root = document.querySelector('#finance-live-data'); if (root) new MutationObserver(() => { enhance(); setupPagination(); }).observe(root, { childList:true, subtree:true });
  enhance(); if (location.pathname === '/weekly') renderActionCenter(); if (location.pathname.startsWith('/opportunities/')) renderDetail();
  document.addEventListener('click', async (event) => { const button = event.target.closest('[data-copy]'); if (!button) return; try { await navigator.clipboard.writeText(button.getAttribute('data-copy') || ''); button.textContent = '已复制'; } catch (_) { button.textContent = '请手动复制'; } });
})();
`;

export const FINANCE_JS = `
(async function () {
  const session = await fetch('/api/finance/auth/session').catch(() => null); if (session && session.status === 401 && location.pathname !== '/login') location.href = '/login';
  const logout = document.querySelector('[data-finance-logout]'); if (logout) logout.addEventListener('click', async (event) => { event.preventDefault(); await fetch('/api/finance/auth/logout', { method:'POST' }); location.href='/login'; });
  const esc = (value) => String(value ?? '').replace(/[&<>"']/g, (char) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char])); const live = document.querySelector('#finance-live-data'); if (!live) return; const fetchJson = async (path) => { const response = await fetch(path); if (!response.ok) throw new Error(path + ' (' + response.status + ')'); return response.json(); }; const empty = (message) => { live.innerHTML = '<div class="finance-empty">' + esc(message) + '</div>'; }; const link = (url, label, cls='finance-link') => url ? '<a class="' + cls + '" href="' + esc(url) + '" target="_blank" rel="noreferrer">' + esc(label) + '</a>' : '';
  const humanizeBlocker = (key) => ({ business_score_below_70:'商业评分不足 70', score_below_70:'商业评分不足 70', missing_contact:'缺少有效联系入口', evidence_gate:'核心证据不足', trigger_gate:'缺少近期有效触发事件', need_gate:'人才需求证据不足', action_gate:'行动方案未完成', evidence_or_contact_gate_fail:'证据或联系入口未通过', outside_top8:'已通过基础条件，但未进入本周 Top 8' }[String(key || '').replace(/=fail$/, '')] || '待人工复核');
  const leadRows = (leads) => (Array.isArray(leads) ? leads : []).map((lead) => { const isA = lead.lead_pool === 'A_ACTIONABLE'; const evidence = (lead.evidences || []).filter((item) => item.source_url).map((item) => '<li>' + link(item.source_url, item.title || '查看原文', '') + '<span class="finance-muted"> · ' + esc(item.source_name) + (item.published_at ? ' · ' + esc(item.published_at) : '') + '</span></li>').join(''); const contacts = [...(lead.contacts || []).filter((item) => item.url || item.email || item.phone).map((item) => '<li>' + esc(item.name || item.title || item.contact_type) + ' ' + link(item.url, 'LinkedIn') + (item.email ? ' <a href="mailto:' + esc(item.email) + '">邮件</a>' : '') + '</li>'), ...(lead.official_contact_entries || []).map((item) => '<li>' + esc(item.label) + ' ' + (item.url ? link(item.url, item.type === 'careers_entry' ? '招聘入口' : '官网联系') : '') + '</li>')].join(''); const script = lead.manual_outreach || lead.first_touch_script_zh || lead.generated_outreach || ''; const reviewLabel = lead.business_review_status === 'human_approved' ? '人工已批准' : isA ? '机器 A 候选' : 'B级情报池'; const blockers = (lead.b_reasons || []).map(humanizeBlocker).map((item) => '<span class="finance-chip warn">' + esc(item) + '</span>').join(''); return '<article class="finance-lead-card ' + (isA ? 'is-a' : 'is-b') + '"><header class="finance-lead-header"><div><h4>' + esc(lead.company_name || '未命名公司') + '</h4><p class="finance-muted">' + esc([lead.industry, lead.region].filter(Boolean).join(' · ') || '公司身份待复核') + '</p>' + (lead.primary_trigger ? '<span class="finance-trigger">' + esc(lead.primary_trigger.title || lead.trigger_summary_zh || '近期触发事件') + '</span>' : '') + '</div><span class="finance-lead-badge ' + (isA ? '' : 'b') + '">' + esc(reviewLabel) + ' <span class="finance-score">' + esc(lead.final_rank_score ?? 0) + '</span></span></header><dl class="finance-lead-details"><dt>为什么现在</dt><dd>' + esc(lead.why_now_zh || lead.opportunity_summary || '待补充业务解释') + '</dd><dt>潜在需求</dt><dd>' + esc(lead.talent_need_zh || '待补充') + '</dd><dt>联系人</dt><dd>' + (contacts ? '<ul>' + contacts + '</ul>' : '<span class="finance-muted">暂无可验证联系入口</span>') + '</dd><dt>本周行动</dt><dd>' + esc(lead.manual_action || lead.bd_action_zh || lead.generated_action || '待补充') + '</dd><dt>首触话术</dt><dd><blockquote>' + esc(script || '暂无首触话术') + '</blockquote>' + (script ? '<button class="finance-link finance-button" data-copy="' + esc(script) + '">复制首触话术</button>' : '') + '</dd>' + (!isA && blockers ? '<dt>阻塞</dt><dd><div class="finance-chip-row">' + blockers + '</div></dd>' : '') + '</dl>' + (evidence ? '<details class="finance-evidence"><summary>证据 ' + esc(lead.evidence_count ?? lead.evidences?.length ?? 0) + ' 条 · 查看证据</summary><ul>' + evidence + '</ul></details>' : '<p class="finance-muted">暂无可验证原文</p>') + '<footer>BusinessScore ' + esc(lead.business_score) + ' · Freshness ' + esc(lead.freshness_score) + ' · 更新 ' + esc(lead.updated_at || '') + '</footer></article>'; }).join('');
  const fillInsight = (snapshot, a, b, recentRun) => { const funnel = snapshot.funnel_metrics || {}; const rows = [['candidate_url_count','候选网页'],['eligible_signal_count','有效 Signal'],['contact_count','联系资料'],['a_count','机器 A 候选']].filter(([key]) => Object.prototype.hasOwnProperty.call(funnel,key)).map(([key,label]) => '<li><span>' + label + '</span><b>' + esc(funnel[key] ?? 0) + '</b></li>').join(''); const blockers = Object.entries(funnel.blocking_reasons || {}).sort(([,x],[,y]) => y-x).slice(0,4).map(([key,value]) => '<li><span>' + esc(humanizeBlocker(key)) + '</span><b>' + esc(value) + '</b></li>').join(''); document.querySelector('[data-funnel]').innerHTML = '<h4>Funnel</h4><ul class="finance-mini-list">' + (rows || '<li><span>暂无</span><b>—</b></li>') + '</ul>'; document.querySelector('[data-blockers]').innerHTML = '<h4>主要阻塞</h4><ul class="finance-mini-list">' + (blockers || '<li><span>暂无阻塞</span><b>—</b></li>') + '</ul>'; document.querySelector('[data-quality]').innerHTML = '<h4>数据质量</h4><p class="finance-muted">' + esc(a + ' 个 A 候选需人工复核；' + b + ' 个 B 级线索保留在情报池。') + '</p>'; const stats = document.querySelector('[data-hero-stats]'); if (stats) { const effectiveRun = recentRun || { status: snapshot.updated_at ? '已发布' : '—', finished_at: snapshot.updated_at }; const runStatus = effectiveRun.status; const runTime = effectiveRun.finished_at ? new Date(effectiveRun.finished_at).toLocaleDateString('zh-CN') : '暂无运行记录'; const items = [[a,'推荐机会','人工复核后联系'],[b,'待验证机会','保留在 B 级情报池'],[runStatus,'最近运行',runTime],[funnel.contact_gate_pass_count ?? 0,'可执行联系','有公开联系入口']]; stats.innerHTML = items.map(([value,label,sub]) => '<article><span>' + label + '</span><strong>' + esc(value) + '</strong><small>' + sub + '</small></article>').join(''); } const week = document.querySelector('[data-week-label]'); if (week) week.textContent = '第 ' + snapshot.week_key + ' 周'; const updated = document.querySelector('[data-updated-label]'); if (updated) updated.textContent = snapshot.updated_at ? '更新于 ' + new Date(snapshot.updated_at).toLocaleString('zh-CN',{hour12:false}) : '已发布'; };
  try { const pathname = location.pathname; if (pathname === '/weekly') { const snapshot = await fetchJson('/api/finance/weekly/current'); if (!snapshot) return empty('暂无已发布周报'); const leads = snapshot.leads || []; const a = leads.filter((lead) => lead.lead_pool === 'A_ACTIONABLE').length; const b = leads.filter((lead) => lead.lead_pool === 'B_ENRICHMENT').length; fillInsight(snapshot,a,b); live.innerHTML = '<div class="finance-section-head"><h3>本周值得联系的机会</h3><span>' + a + ' 个 A · ' + b + ' 个 B</span></div>' + (leadRows(leads) || '<div class="finance-empty">本期没有达到展示门槛的线索</div>'); } else if (pathname === '/leads/a' || pathname === '/leads/b') { const leads = await fetchJson('/api/finance/leads/' + (pathname.endsWith('/a') ? 'a' : 'b')); live.innerHTML = '<div class="finance-section-head"><h3>' + (pathname.endsWith('/a') ? 'A级机会' : 'B级情报池') + '</h3><span>' + leads.length + ' 条</span></div>' + (leadRows(leads) || '<div class="finance-empty">暂无数据</div>'); } else if (pathname === '/opportunities') { const rows = await fetchJson('/api/finance/opportunities'); live.innerHTML = '<div class="finance-section-head"><h3>Opportunity Pipeline</h3><span>' + rows.length + ' 条</span></div><table class="finance-table"><thead><tr><th>机会</th><th>公司</th><th>状态</th><th>Why Now</th><th>下一步</th></tr></thead><tbody>' + (rows.map((item) => '<tr><td><strong>' + esc(item.title) + '</strong></td><td>' + esc(item.company_name || item['company_'+'id']) + '</td><td>' + esc(item.status) + '</td><td>' + esc(item.why_now) + '</td><td>' + esc(item.next_action) + '</td></tr>').join('') || '<tr><td colspan="5">暂无已生成机会</td></tr>') + '</tbody></table>'; } else if (pathname === '/watchlist') { const rows = await fetchJson('/api/finance/watchlist'); live.innerHTML = '<div class="finance-section-head"><h3>Watchlist</h3><span>' + rows.length + ' 家公司</span></div><table class="finance-table"><thead><tr><th>公司</th><th>状态</th><th>优先级</th><th>最近观察</th></tr></thead><tbody>' + (rows.map((item) => '<tr><td><strong>' + esc(item.company?.canonical_name || item['company_'+'id']) + '</strong></td><td>' + esc(item.status) + '</td><td>' + esc(item.priority) + '</td><td>' + esc(item.last_snapshot_week || '等待首期 Snapshot') + '</td></tr>').join('') || '<tr><td colspan="4">暂无 Watchlist 公司</td></tr>') + '</tbody></table>'; } else if (pathname === '/companies') { const companies = await fetchJson('/api/finance/companies'); live.innerHTML = '<div class="finance-section-head"><h3>公司库</h3><span>' + companies.length + ' 家</span></div><table class="finance-table"><thead><tr><th>公司</th><th>细分</th><th>状态</th><th>最近验证</th></tr></thead><tbody>' + (companies.map((c) => '<tr><td><strong>' + esc(c.canonical_name) + '</strong></td><td>' + esc(c.target_segment || '—') + '</td><td>' + esc(c.status || '—') + '</td><td>' + esc(c.last_verified_at || '—') + '</td></tr>').join('') || '<tr><td colspan="4">暂无公司</td></tr>') + '</tbody></table>'; } else if (pathname === '/runs') { const runs = await fetchJson('/api/finance/runs'); live.innerHTML = '<div class="finance-section-head"><h3>雷达运行</h3><span>' + runs.length + ' 次</span></div><table class="finance-table"><thead><tr><th>Run</th><th>状态</th><th>公司</th><th>Lead</th><th>更新时间</th></tr></thead><tbody>' + (runs.map((run) => '<tr><td><strong>' + esc(run.radar_run_id) + '</strong></td><td>' + esc(run.status) + '</td><td>' + esc(run.company_count ?? '—') + '</td><td>' + esc(run.lead_count ?? '—') + '</td><td>' + esc(run.finished_at || '—') + '</td></tr>').join('') || '<tr><td colspan="5">暂无运行记录</td></tr>') + '</tbody></table>'; } else if (pathname === '/trends') { const trends = await fetchJson('/api/finance/trends'); live.innerHTML = '<div class="finance-section-head"><h3>趋势</h3><span>' + trends.length + ' 条</span></div>' + (trends.map((t) => '<article class="finance-lead-card"><h4>' + esc(t.title) + '</h4><p class="finance-muted">' + esc(t.summary) + '</p></article>').join('') || '<div class="finance-empty">暂无趋势</div>'); } } catch (error) { empty('数据加载失败：' + (error?.message || '未知错误')); }
  document.addEventListener('click', async (event) => { const button = event.target.closest('[data-copy]'); if (!button) return; await navigator.clipboard.writeText(button.getAttribute('data-copy') || ''); button.textContent='已复制'; }); const refresh = document.querySelector('[data-refresh]'); if (refresh) refresh.addEventListener('click', () => location.reload());
})();
`;

# V1.2.1 Precision / Cost Hotfix Handoff

Date: 2026-09-03  
Branch: `feat/headhunter-finance-mvp-phase5`  
Commit: `a59d033a79a58a020d01ed4afb9cf791adc386ec`

## Delivery status

- TikHub is not a Finance Radar provider. Runtime, scheduler, manual, golden,
  and production smoke paths are guarded to zero TikHub calls.
- Trigger, entity/region, job employer, official contact, mailbox, UI-label,
  and machine-A review-status hotfixes are implemented.
- Historical TikHub cost is retained as read-only evidence. The prior
  operator-confirmed `105 × USD 0.05 = USD 5.25` is marked `USER_CONFIRMED`;
  unknown billing rows remain unknown.
- Doubao production secret was already configured in
  `/etc/chanceping/chanceping.env`; this release does not print or commit it.

## Precision result

The offline controlled 20-company run `v121-golden-20` passed with 20/20
company, trigger, job, people and contact fixture records and 0 TikHub
requests. Automatic records remain labelled `machine_candidate`; no human
approval or routing change is implied. The prior human review remains the
production precision baseline: company identity 18/20 pass, 1 review, 1 fail;
signal 3/20 pass, 3 review, 14 fail.

## Verification

Passed: `npm run typecheck`, `npm run verify:headhunter:v121`,
`npm run verify:headhunter`, `npm run verify:all`,
`./scripts/verify-deploy-release.sh`, and read-only production smoke checks.

## Deployment

Production URL: [finance.chanceping.com](https://finance.chanceping.com)  
Release: `20260903T100835Z-a59d033a79a5`  
Health: `/health` returned HTTP 200 and systemd service is active.  
Remote release manifest reports status `healthy` and commit
`a59d033a79a58a020d01ed4afb9cf791adc386ec`.

Production routing remains unchanged. Phase 4 / final adoption approval is
not inferred from this hotfix; a future human re-judgment is still required.

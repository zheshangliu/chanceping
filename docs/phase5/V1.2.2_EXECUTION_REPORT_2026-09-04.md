# 维优猎头 BD 雷达 V1.2.2 执行报告

日期：2026-09-04（Asia/Shanghai）
范围：`目标模式执行补充` + `Eligibility 与 HistoricalDataHygiene 开发任务书`
TikHub：本轮未调用；此前 `search_jobs` 已按成本价值 Gate 停止。

## 结论

代码实施完成，静态检查、猎头域回归、V1.2.2 golden 与发布原子性检查通过。已完成同一冻结 20 家公司的真实 Provider 生产式 `publish=false` 运行，但业务质量门槛尚未达到：当前有效 Signal=0、有效 Person=0、机器 A=0/20，因此不批准修改生产 routing，也不发布新的周报。

## Run

- `run_id`: `headhunter-v122-production-style-20260904-frozen`
- `week_key`: `2026-W36`
- `publish`: `false`
- 冻结样本：V1.2.1 真实复审的同 20 家公司
- Provider：Serper 96 requests / 0 failures；Doubao 31 requests / 0 failures；TikHub 0
- Provider cost：`unknown`（当前 Provider usage 未提供可审计单价；未按 0 计算）

## Eligibility funnel

| 层 | 原始 | 有效 |
|---|---:|---:|
| Signal | 0 | 0 |
| Job | 16 | 16 |
| Person | 51 | 0 |
| Contact | 68 | 56 |

过滤记录总数：63
过滤原因：`stale=7`、`entity_mismatch=43`、`employer_mismatch=10`、其余 `0`。

## 业务结果

- Company resolved：20/20（运行成功；同一冻结样本）
- Job/Need：有效 Job 16，Need 13；未改变 RA1 / Cantonese literal-only 规则
- Contact Gate：56 条有效公开联系入口
- Lead：20（A=0，B=20）
- Machine A precision：N/A（A=0）；没有使用 LLM 自动代替人工 Judgment
- Freshness：以最新 eligible `event_date` 计算；不存在用 `last_seen_at` 冒充事件日期的路径

## Safety / hygiene

- 原始集合与 `eligible*` 集合分离，历史污染只过滤、不删除 raw evidence。
- 已隔离 entity/region/employer 不一致、过期与失效任职记录。
- 第一方页面抓取具备超时、最大字节数、HTML content-type、重定向上限。
- HKIB `recruit@hkib.org` 与 GBA Pharma BD 入口 golden case 通过。
- BusinessScore、FinalRankScore、Hard Gate、Serper/Doubao routing 未改；TikHub 未重新调用。

## 检查结果

- `npm run typecheck`：PASS
- `npm run verify:headhunter`：PASS
- `npm run verify:headhunter:v122`：PASS
- `bash scripts/verify-deploy-release.sh`：PASS（包含健康失败自动回滚演练）
- `npm run verify:all`：PASS（包含 `verify-task034` / `verify-task025` 根路由兼容断言）。

## 决策

`Eligibility Code Gate=PASS`；`Regression Gate=PASS`；`Live Data Gate=PASS (publish=false, 20/20 run completed)`；`Business Quality Gate=FAIL/HOLD`。保持生产 W37 与默认路由不变；下一轮应补充真实事件日期/可验证当前人员数据后再做人工 precision judgment 和 routing review。

## 发布状态

本地提交：`000f3b7`。受控 push 已尝试 HTTPS 与 SSH-over-443，均因当前环境连接 GitHub 443 超时失败；因此 GitHub Actions 未触发，生产未部署，线上 W37 保持不变。网络恢复后应从本分支继续执行同一 Push/Deploy Skill 流程。

# 维优猎头 BD 雷达 V1.2.2 执行报告

日期：2026-09-04（Asia/Shanghai）
范围：`目标模式执行补充` + `Eligibility 与 HistoricalDataHygiene 开发任务书`
TikHub：本轮未调用；此前 `search_jobs` 已按成本价值 Gate 停止。

## 结论

代码实施完成，静态检查、猎头域回归、V1.2.2 golden 与发布原子性检查通过。已完成同一冻结 20 家公司的真实 Provider 生产式 `publish=false` 运行，但业务质量门槛尚未达到：当前有效 Signal=1、有效 Person=33、有效 Job=13，机器 A=0/20，因此不批准修改生产 routing，也不发布新的周报。

## Run

- `run_id`: `headhunter-v122-production-style-20260904-frozen-v6`
- `week_key`: `2026-W36`
- `publish`: `false`
- 冻结样本：V1.2.1 真实复审的同 20 家公司
- Provider：Serper 138 requests / 0 failures；Doubao 30 requests / 0 failures；Exa 8 requests / 0 failures；TikHub 0
- Provider cost：`unknown`（当前 Provider usage 未提供可审计单价；未按 0 计算）

## Eligibility funnel

| 层 | 原始 | 有效 |
|---|---:|---:|
| Signal | 1 | 1 |
| Job | 16 | 13 |
| Person | 126 | 33 |
| Contact | 43 | 43 |

过滤记录总数：97
过滤原因：以 `entity_mismatch` 为主（具体计数保存在 RadarRun 的 `ineligible_by_reason`），其余为 `0` 或由同一候选的多阶段重验合并计数。

## 业务结果

- Company resolved：20/20（运行成功；同一冻结样本）
- Job/Need：原始 Job 16、有效 Job 13，Need 12；未改变 RA1 / Cantonese literal-only 规则
- Person：原始候选 126、有效当前任职 33；本轮未调用 TikHub User Profile
- Contact Gate：43 条有效公开联系入口
- Lead：20（A=0，B=20）
- Machine A precision：N/A（A=0）；未使用 LLM 自动代替人工 Judgment
- Freshness：以最新 eligible `event_date` 计算；不存在用 `last_seen_at` 冒充事件日期的路径

## Safety / hygiene

- 原始集合与 `eligible*` 集合分离，历史污染只过滤、不删除 raw evidence。
- 已隔离 entity/region/employer 不一致、过期与失效任职记录。
- 第一方页面抓取具备超时、最大字节数、HTML content-type、重定向上限。
- HKIB `recruit@hkib.org` 与 GBA Pharma BD 入口 golden case 通过。
- BusinessScore、FinalRankScore、Hard Gate、Serper/Doubao routing 未改；TikHub 未重新调用。

## 检查结果

- `npm run typecheck`：PASS
- `npm run verify:headhunter`：PASS
- `npm run verify:headhunter:v122`：PASS
- `bash scripts/verify-deploy-release.sh`：PASS（包含健康失败自动回滚演练）
- `npm run verify:all`：PASS（包含 `verify-task034` / `verify-task025` 根路由兼容断言）。

## 决策

`Eligibility Code Gate=PASS`；`Regression Gate=PASS`；`Live Data Gate=PASS (publish=false, 20/20 run completed)`；`Business Quality Gate=FAIL/HOLD`。Company 20/20 与 Contact 43/43 达到本轮下限，但 Signal 1/20、Job/Need 11/20 未达到目标；A=0 按规则为 N/A。追加真实品牌名查询后，事件库存仍仅有 1 个可进入 eligible Signal 的目标公司记录；若要达到 Signal 14/20，必须改变 Provider/查询预算、冻结样本或判定门槛，已触发业务质量 Stop/Escalation，不得静默放宽规则。保持生产 W37 与默认路由不变。

## 发布状态

本地提交：`171159b`，已受控 push 至 `feat/headhunter-finance-mvp-phase5`。Finance 分支 CI `33827555416` 已通过（typecheck、V1.5 回归、verify:all、发布原子性检查）。生产 workflow dispatch 返回 `HTTP 404: deploy-production.yml not found on the default branch`，因此生产 GitHub Actions 未触发；且本轮 Business Quality Gate 为 HOLD，未使用 direct recovery 路径部署。线上 W37 保持不变。

# ChancePing V1.5 Wayne Production Readiness Handoff

## Completion Status

`PASS` — V1.5 readiness implementation, regression verification, controlled production deployment, and remote readiness verification are complete on the Finance branch. Product/business review remains a separate follow-up.

## Task

Make the existing V1.5 custom-radar path safe for Wayne's production Finance environment. Preserve the eight completed V1.5 capabilities (model, stores, API, UI, AI generator, scheduling, quota, and report binding), verify restart/scheduler/failure behavior, and enforce the current public Finance contract: public reads are allowed, anonymous mutations and high-cost legacy endpoints are not.

## Branch / Commit

Branch: `feat/headhunter-finance-mvp-phase5`  
Implementation commit SHA: `86810bfc8e88b7e0e9563163b62a424bd154818f`  
Readiness verifier commit: `d58d3e4`  
Handoff metadata commit: `c7ae82f`.

## Implemented

- Added a host-aware public Finance boundary in `src/api/app.ts`.
- On `finance.chanceping.com` with `FINANCE_PUBLIC_MODE=true`, `GET`/`HEAD`/`OPTIONS` remain available while non-Finance API mutations return `FINANCE_PUBLIC_READ_ONLY` with HTTP 401.
- Finance API mutations remain governed by the existing Finance session guard.
- Main ChancePing host behavior remains backward compatible for the anonymous MVP and existing V1.5 tests.
- Added `scripts/verify-headhunter-v15-production-readiness.ts` and `verify:headhunter:v15:readiness`.
- The readiness verifier covers public-read/mutation boundaries, persistence across a fresh store instance, Monday 07:00 Asia/Shanghai scheduler registration, and failed snapshot isolation.

## Architecture Impact

- No data model or provider routing changes.
- No TikHub calls are introduced.
- The guard is applied before route handlers and is scoped to the Finance host, preventing anonymous provider-spend and write access through global legacy APIs without changing the main site.
- Existing public Finance GET APIs remain served from the persistent HeadHunter stores.

## Business Impact

Wayne can continue reading the public weekly brief and Finance workbench pages. An unauthenticated request cannot create/run a radar, execute a generic search, generate a report, or mutate the Finance watchlist from the Finance host. Restart persistence and safe failed-run behavior are verified before any release decision.

## Tests

- `npm run typecheck` — PASS
- `npm run verify:headhunter:v15:readiness` — PASS (public read-only, mutation boundary, persistence restart, weekly scheduler, failed snapshot isolation)
- `CHANCEPING_DEPLOY_BASE_URL=https://finance.chanceping.com npm run verify:headhunter:v15:readiness` — PASS (remote public reads, Finance mutation guard, and legacy high-cost mutation boundary)
- `npm run verify:v15` — PASS (all eight V1.5 task verifiers)
- `npm run verify:v15:e2e` — PASS
- `npm run verify:v16` — PASS
- `npm run verify:all` — PASS
- `bash scripts/verify-deploy-release.sh` — PASS
- `CHANCEPING_DEPLOY_BASE_URL=https://finance.chanceping.com npm run verify:headhunter:production-smoke` — `READ_ONLY_PASS`

## Deployment

Finance branch was pushed and deployed through the Push Skill direct recovery path after the governed GitHub workflow was confirmed unavailable. Production release: `20260904T235506Z-b7c49d104a61`; commit: `b7c49d104a618a4899a7a09876d2be67eed71dea`; previous release: `20260904T064652Z-32118a2`. The release passed `/health` and the post-deploy remote smoke. The V1.5 readiness verifier then passed the public-read and mutation-boundary checks against `https://finance.chanceping.com`.

## Known Issues

- Local Chromium visual QA remains blocked by the existing `spawn Unknown system error -88`; this readiness work relies on static/contract and remote HTTP evidence, not a claim of pixel-perfect visual approval.
- The protected GitHub deploy workflow is still absent from the default branch (`deploy-production.yml`); the documented direct recovery path is available when deployment is explicitly approved.
- The legacy production smoke still reports eight failures because it expects anonymous writes and a built-in navigator mutation; those assertions conflict with the V1.5 public read-only contract. The dedicated V1.5 readiness verifier is the authoritative boundary check.
- The existing V1.5 quota uses the current demo-user configuration; replacing it with account authentication is outside this readiness patch.

## Recommended Next Step

Review the deployed V1.5 Finance workbench and decide whether to proceed with the separate product/business adoption review; no routing changes were made.

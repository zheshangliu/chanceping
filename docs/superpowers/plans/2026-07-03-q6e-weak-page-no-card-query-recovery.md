# Q.6-E Weak Page No-Card Query Recovery Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tighten weak-page downgrades, make no-card reports useful, and improve dynamic query recovery for AI/startup and cross-border marketplace opportunities.

**Architecture:** Keep Q.6-E as a small quality patch. Page classification stays in `src/search/candidate-page-type.ts`, judge hard guards stay in `src/search/candidate-llm-judge.ts`, report copy stays in `src/agents/radar-report-generator.ts`, and query recovery stays in `src/search/opportunity-strategy.ts`.

**Tech Stack:** TypeScript, existing Node scripts, live Serper/DeepSeek only for explicit validation.

---

### Task 1: Weak Page Downgrade

**Files:**
- Modify: `scripts/verify-q6d-page-type-beneficiary.ts`
- Modify: `src/search/candidate-page-type.ts`
- Modify: `src/search/candidate-llm-judge.ts`

- [ ] **Step 1: Add failing tests**
  - Add fixture checks for `about_us`, `institution_profile`, `information_disclosure`, and `platform_intro`.
  - Add LLM-upgrade-attempt checks to ensure these page types cannot become accepted key cards.

- [ ] **Step 2: Run red test**
  - Run: `node --run verify:q6d`
  - Expected: FAIL for missing weak page classification/guard.

- [ ] **Step 3: Implement minimal page-type classification and judge hard guards**
  - Add new `CandidatePageType` variants.
  - Classify weak pages before action-entry types.
  - Prevent LLM from upgrading downgraded weak pages.

- [ ] **Step 4: Run green test**
  - Run: `node --run verify:q6d`
  - Expected: PASS.

### Task 2: No-Card Report Expression

**Files:**
- Modify: `scripts/verify-report-template.ts`
- Modify: `src/agents/radar-report-generator.ts`

- [ ] **Step 1: Add failing report assertions**
  - Build a report with `opportunities=[]` and downgraded raw candidates.
  - Assert the report says no key card was found, lists observation signals, explains downgrade reasons, and gives next query/source directions.

- [ ] **Step 2: Run red test**
  - Run: `node --run verify:report-template`
  - Expected: FAIL until no-card section exists.

- [ ] **Step 3: Implement no-card observation section**
  - Add a concise no-card block in action/report sections.
  - Do not turn observations into opportunity cards.

- [ ] **Step 4: Run green test**
  - Run: `node --run verify:report-template`
  - Expected: PASS.

### Task 3: Dynamic Query Recovery

**Files:**
- Modify: `scripts/verify-q6d-page-type-beneficiary.ts`
- Modify: `src/search/opportunity-strategy.ts`

- [ ] **Step 1: Add failing query assertions**
  - Cross-border marketplace queries include seller registration, Shopee/Lazada/TikTok/Amazon Global Selling, fulfillment/overseas warehouse.
  - AI/startup queries include AI application contest, Qwen/Alibaba Cloud/AWS/Google Cloud/Microsoft for Startups.

- [ ] **Step 2: Run red test**
  - Run: `node --run verify:q6d`
  - Expected: FAIL until variants are expanded.

- [ ] **Step 3: Implement minimal semantic recovery**
  - Extend existing regex-triggered recovery without per-industry if/switch templates.
  - Keep hard caps intact.

- [ ] **Step 4: Run green test**
  - Run: `node --run verify:q6d`
  - Expected: PASS.

### Task 4: Validation And Commit

**Files:**
- Modify reports generated by validation as needed.

- [ ] **Step 1: Run required checks**
  - Run: `node --run typecheck`
  - Run: `node --run verify:q6d`
  - Run: `node --run verify:q6`
  - Run: `node --run verify:q5`
  - Run: `node --run verify:api-env`
  - Run: `node --run verify:q3`
  - Run: `node --run verify:live-provider-health`
  - Run: `node --run verify:live-llm`
  - Run: `node --run verify:live-mvp`
  - Run: `node --run verify:mvp-browser`
  - Run: `node --run verify:all`
  - Run: `git diff --check`

- [ ] **Step 2: Run Q.6 selected live diagnostic**
  - Start Q4 live server with explicit local live env.
  - Run selected live diagnostic only, not Golden full.

- [ ] **Step 3: Commit**
  - Commit message: `Q.6-E: downgrade weak pages and improve no-card reports`

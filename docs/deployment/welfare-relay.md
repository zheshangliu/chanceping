# 福利雷达兼容中继

当 SWAS 的本机 TLS 客户端无法与深圳政务网完成 HTTP 数据传输时，福利采集器会在 Node HTTPS、curl、GnuTLS 都失败后，使用受限的 Reader 兼容中继读取**公开官方 URL**。

- 中继仅是传输兼容层；卡片、字段证据和跳转链接始终使用官方详情 URL。
- 公开 API 不返回中继地址、原始快照路径或内部错误。
- 默认仅允许 `https://r.jina.ai/http://`；不允许通过环境变量替换为任意地址。
- 若要完全关闭兼容中继，可在服务器环境设置一个无效值；采集会诚实地标记来源失败，而不会伪造数据。

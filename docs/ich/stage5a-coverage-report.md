# Stage 5-A 机会覆盖报告（第一批）

统计口径：2026-09-06 12:00（Asia/Shanghai），仅统计已发布且 `computeIchOpportunityStatus` 为 `active`、`closing_soon` 或 `long_term` 的记录。

## 当前覆盖

| 主分类 | Active 数量 |
|---|---:|
| 比赛/奖项（`competition`） | 14 |
| 展览/市集（`exhibition_market`） | 5 |
| 渠道合作（`channel_collaboration`） | 2 |
| 国际交流（`international`） | 2 |
| 资助扶持（`policy_funding`） | 3 |
| 采购项目（`procurement_project`） | 0 |
| **合计** | **26** |

## 与 Stage 5-A 目标的差距

当前模型的正式主分类仍需继续细分或补充采购、驻地、教育、品牌联名等机会。现阶段不能把“国际”或“展览”记录重复计入多个主分类来制造覆盖达标；后续批次优先补充采购项目、渠道合作、驻地/教育合作和 `heritage_program`，同时保持每条记录的官方证据与生命周期判断。

## 跨雷达标签

本批新记录均写入 `radar_tags`，使用 `ICH`、`Craft`、`Culture`、`Design`、`Business`、`Funding`、`International` 等标签；标签用于跨雷达分发，不改变主分类和去重规则。

# 非遗机会雷达阶段 4 浏览器验收记录 V1.0

验收日期：2026-07-24

验收环境：本地隔离服务，独立临时机会库、提交队列、事务日志和测试管理令牌。

## 验收结果

| 场景 | 结果 | 证据 |
|---|---|---|
| 桌面端来源提交页 | 通过 | 页面标题、说明、四个可见字段、蜜罐字段、提交按钮和信息原则完整呈现 |
| 来源提交交互 | 通过 | 填写 HTTPS 官方链接、标题、说明和测试邮箱后，页面显示“已收到，我们会进行人工核验。” |
| 表单清空 | 通过 | 成功提交后可见字段被清空 |
| 管理后台页面 | 通过 | 机会审核区和来源提交队列区均存在，管理令牌不嵌入 HTML |
| 移动端 390×844 | 通过 | 首屏无横向溢出，正文和表单宽度正常，字段与按钮可见 |
| 响应式 DOM | 通过 | viewport 390px、body 390px、main/card 342px、document scroll width 390px |
| 浏览器控制台 | 通过 | 未观察到影响提交路径的页面错误 |

## 数据安全

- 使用 `127.0.0.1:3317` 隔离实例；
- 测试数据只写入 `/tmp` 下的临时目录；
- 未读取或修改生产机会库；
- 未使用生产管理令牌；
- 验收结束后停止服务并删除临时目录；
- 未将测试邮箱、令牌或提交数据写入仓库。

## 关联自动化证据

- `npm run verify:ich:stage4:seo`
- `npm run verify:ich:stage4:submissions`
- `npm run verify:ich:stage4:integration`
- `npm run verify:ich:stage4`

自动化覆盖 canonical、OG、JSON-LD、sitemap、robots、HTTPS URL 校验、
请求体上限、蜜罐、表单停留时间、限流、匿名鉴权、分页、接受、拒绝、待处理、
事务恢复、草稿公开隔离和重启持久化。

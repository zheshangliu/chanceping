# 盯非遗｜1950fb V1.1 定点收尾报告

日期：2026-09-08（Asia/Shanghai）
基线：`1950fb993daeb34a06e6a41a0c930ebc65771d01`
分支：`rescue/mvp-codex`

本轮执行依据为压缩包内《盯非遗_1950fb验收与V1.1定点收尾清单.md》。已读取 README、主任务书，并用 `references/旧版页面视觉基准.pdf` 与截图作为旧 UI 基准。未部署生产、未修改 DNS、未新增 Source，未引入评分或官方核验门槛。

## 已完成

- 外语展示链路改为真实翻译状态：移除词替换和固定模板摘要；缓存按原文 hash、目标语言和策略版本复用；翻译失败时保留原文并显示待补/失败状态；译文搜索字段已纳入 V2 查询。
- 查询语义统一：默认可浏览包含未来开始、长期开放和截止待确认；`current` 排除未来开始；`closing_soon` 排除未来开始；非法日期返回 `UNKNOWN_DEADLINE`；线上筛选不再把未知参与方式当成线上。
- 日期表示统一使用来源日期日历值，避免 `23:59:00Z` 在北京时间展示时偏移一天；补充了无年份中文日期、Unicode hyphen 和非法日期回归测试。
- 赛事备忘录增加搜索表单并保留方向、地区、状态、作品形式等查询条件；代码分类键在备忘录中显示为中文；实际 101 条备忘录 ID 与 13 页卡片视图并集一致。
- 来源请求增加逐跳公网 DNS 校验、批准 IP 绑定、实际响应体大小限制和超时；分页中途失败/预算耗尽返回 `partial` 与 `next_page`；source、pool、health JSON 写入加入最小跨进程锁和原子替换；新增来源追加操作使用锁内读改写。
- 旧纸张视觉、首页卡片分页、详情、后台 Source Manager 与 CFW/V2 主流程保留。

## 中文处理实测

仓库已有 contest profile 配置，实际解析到 `qwen:qwen3.7-plus`。本轮真实调用覆盖机会池中 231 条外语候选，服务返回 HTTP 403：free quota exhausted。因此实际结果为：

```text
foreign records: 231
current foreign radar records: 30
translated: 0
reused: 0
pending: 0
failed: 231（其中当前可浏览 30 条均为 failed）
current Chinese display: 0
```

这不是代码回退：失败记录均保留原始标题/摘要，页面标记“中文翻译失败，当前显示原文”；没有把模板或词替换写成 `translated`。待 Qwen 配额恢复后，重新运行 `npm run opportunity:v2:display` 即可按 hash 增量补齐。

## 本地数据快照

本轮未执行完整 Opportunity V2 抓取，因此以下是本地现有快照，不冒充新抓取结果：

```text
sources: 30
pool: 728
default radar: 146
competition radar: 101
CN source-region: 125
GLOBAL source-region: 21
source status: ACTIVE 20 / FAILED 6 / NEEDS_ADAPTER 4
```

CN/GLOBAL 是来源地区口径，不等同于赛事所在地统计。没有为 cnyisai 新增 Source。

## 来源只读核验

对既定候选 `https://www.cnyisai.com/calls.html` 做了只读访问：HTTP 200、`text/html; charset=UTF-8`、97,494 bytes、页面标题为“全部设计大赛_艺术赛事征集列表_国内国际赛事报名-艺赛中国”。现有 Generic Listing 解析出 101 个候选锚点；抽样可见真实赛事与“赛事获奖”导航/结果内容混杂，说明该入口可访问但仍需要专用清洁规则后才能接入。本轮按“不新增 Source”要求不接入、不写入 Opportunity Pool。

## 回归与截图

通过：

- `npm run verify:ich:1950`
- `npm run verify:ich:v11`
- `npm run verify:opportunity:v2`
- 工作区 `npm run typecheck` 会被未跟踪的 benchmark/headhunter 文件阻断；将本提交隔离检出到干净临时目录后，`npm run typecheck` 通过。
- 真实备忘录 101 IDs = 13 页卡片视图并集 101 IDs
- Qwen 真实调用失败路径：原文保留、failed 可追踪

预览文件位于 `docs/ich/previews/1950fb-finish/`，包含桌面/390×844 手机首页与备忘录、海外+AIGC+作品形式组合筛选、LOEWE 详情、Source Manager 后台。截图不包含 token；由于翻译服务配额阻断，LOEWE 详情以原文和失败状态呈现，这一项不宣称中文验收通过。

## 未完成与阻断

1. Qwen free quota exhausted，231 条外语候选尚未获得真实中文译文；这是外部配置/配额阻断，不使用假翻译掩盖。
2. 本轮未重新抓取全量 V2，因此 pool/radar 数字仍是本地快照；cnyisai 只做只读核验，没有新增 Source。
3. 原工作区存在与本任务无关的 benchmark/headhunter 等未跟踪文件；提交时只暂存本报告列出的相关文件。

生产部署：NO
DNS 修改：NO

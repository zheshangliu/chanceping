# DS2 只读发现运行报告 V1.0

## 1. 运行结果

运行记录：[DS2-只读发现运行记录_V1.0.json](/Users/1sunflower/Documents/chanceping/docs/ich/DS2-只读发现运行记录_V1.0.json)。

| 指标 | 结果 |
|---|---:|
| 来源运行数 | 3 |
| 候选样本数 | 9 |
| 中国政府采购网 | completed / 3 条 |
| 广东省文化和旅游厅 | completed / 3 条 |
| 越秀区通知公告 | completed / 3 条 |
| 正式库写入 | `false` |
| 正式库快照是否变化 | 否 |
| DS2 门禁 | `pass` |

## 2. 证据完整性

- 每个来源均记录列表页 HTTP 状态、最终 URL、列表快照 SHA-256 和错误列表；
- 每条候选均保留详情页 URL、字段 provenance、`candidate_only` 和详情快照 SHA-256；
- 运行前后正式库哈希一致；
- 没有登录、表单提交、导入、发布或部署操作。

## 3. DS2 结论

DS2 的只读发现运行层通过首轮门禁，可以重复运行并输出候选证据。它不改变 DS1-D 的批准状态，也不把候选自动升级为正式机会。DS1-D 的 1 条待批准草稿仍需用户明确允许后才能导入。

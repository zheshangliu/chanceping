# 非遗机会雷达阶段 5 真实数据首发验收报告 V1.0

验收时间：2026-07-24（Asia/Shanghai）
导入批次：`ich-stage5-2026-07-24`
结论：本地验收通过，尚未提交、推送或部署。

## 1. 数据结果

| 指标 | 结果 | 门槛 |
|---|---:|---:|
| 原始候选筛查 | 55 | 50—70 |
| 当前有效公开机会 | 30 | ≥30 |
| 历史机会 | 5 | ≥5 |
| L1/L2 主要来源 | 93.33% | ≥80% |
| 重复发现组 | 6 | ≥2 |
| 无效或重复导入 | 0 | 0 |

六类当前机会：

- 比赛与作品征集：8；
- 展会、市集与展销：5；
- 采购、项目与活动需求：5；
- 渠道入驻与品牌合作：3；
- 政策、资金与人才计划：3；
- 国际交流与海外机会：6。

## 2. 真实性处理

- 已截止、结果公告、活动报道、缺少行动入口和无法确认日期的条目没有计入当前 30 条；
- 未公开金额、费用和布尔条件保持 `null` 或明确“未公开”；
- 具体机会的来源等级按页面判定，不继承来源登记表的建议等级；
- `zero material` 已通过 Kakilang 第一方页面复核，来源升级为 L2；
- 石湾陶塑培训采购保留为 L3 线索并标记 `partially_verified`，用户申请前须向采购单位复核原始通告，未伪装为 L1/L2。

## 3. 导入与回滚

- 候选文件：`src/ich/opportunities.stage5-candidates.json`；
- 筛查台账：`src/ich/stage5-candidate-ledger.json`；
- 正式数据：`src/ich/opportunities.verified.json`；
- 导入器默认 dry-run，只有显式传入 `--write` 且全部门槛通过才写入；
- 写入前由 store 自动创建 `.bak` 并采用临时文件原子替换；
- 本轮备份已在核对正式文件后移除，Git 提交前可通过上一版本的
  `src/ich/opportunities.verified.json` 完整回滚；
- 若发布后需回滚，应恢复发布前该文件，重新执行验证，再按限定范围发布流程部署。

## 4. 自动验收

- `npm run typecheck`：通过；
- `npm run verify:ich:stage5`：18/18；
- `npm run verify:all`：通过，包含 ICH 阶段 1—5、V1.5 E2E、V1.6 和现有全量回归；
- `git diff --check`：通过。

## 5. 本地产品路径

- `/ich`：HTTP 200，初始 HTML 包含真实机会标题；
- 公开列表 API：30 条，状态为 19 条 `active`、11 条 `closing_soon`；
- `procurement_project` 筛选：5/5 条分类一致；
- 耀州瓷详情页：HTTP 200，并包含真实标题；
- `/ich/history`：HTTP 200，显示历史机会和已截止语义。

## 6. 发布边界

本轮未执行 Git 暂存、提交、推送、合并或部署。阶段 5 数据已经达到本地发布候选状态，
但仍需产品负责人审阅数据范围，并单独授权后才能使用限定范围提交与发布流程。

# DS8 受控批量发布与生命周期设计 V1.0

DS8 将机会从“候选”提升到“正式库”时的安全阀固化为代码。批量发布上限为 10 条，候选必须已经过人工审核并处于 `approved`，不能重复、不能已发布、来源必须可访问，且动态状态不能已过期或结束。

生命周期审计会识别动态状态、待复核来源、已发布主来源重复和需要撤回的记录。审计阶段只读；实际晋级仍沿用 DS1-D 工作流并生成原子备份。

如果存在大量 `needs_recheck` 记录，门禁会标记为 `pass_with_followups`，不能把它误读为所有来源都已新鲜核验；后续运行必须优先处理临近截止和来源待复核记录。
